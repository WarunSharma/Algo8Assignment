//Created a config.js with devlopement and production configurations

//Can save config file by creating an object like 
    const config=export('./config')
    let configuration=new config();

//Can put DB and URI settings    